# Puertos publicados en el host (cambiar si 8080/8090/1883 están en uso, ej. en Dokploy)
# ChirpStack UI (por defecto 8080). Si 8080 está ocupado, usar ej. 8081.
CHIRPSTACK_UI_PORT=8080
# ChirpStack REST API (por defecto 8090). Si 8090 está ocupado, usar ej. 8091.
CHIRPSTACK_REST_PORT=8090
# Mosquitto MQTT (por defecto 1883)
MOSQUITTO_PORT=1883

# Ejemplo para Dokploy cuando 8080 y 8090 están usados:
# CHIRPSTACK_UI_PORT=8081
# CHIRPSTACK_REST_PORT=8091
